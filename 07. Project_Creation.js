function timeForAProjects(name_ar, projects) {
    let time_for_projects = projects * 3;
    console.log(`The architect ${name_ar} will need ${time_for_projects} hours to complete ${projects} project/s.`);
}
timeForAProjects("Stefan", "5")